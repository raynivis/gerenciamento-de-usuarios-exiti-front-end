function HeaderUsuarios() {
     return (
          <>
               <div className="p-4 bg-white">
                    <button className="bg-primary hover:bg-secondary text-white px-4 py-2 rounded text-sm font-medium flex items-center gap-2 transition-colors">
                         + ADICIONAR USUÁRIO
                    </button>
               </div>
          </>
     );
}

export default HeaderUsuarios;
